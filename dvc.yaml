stages:
  train:
    cmd: python train.py
    deps:
      - train.py
      - data/iris_v1.csv
      - data/iris_v2.csv
    params:
      - params.yaml:
          - train
    outs:
      - models/model.pkl
    metrics:
      - metrics.json:
          cache: false
