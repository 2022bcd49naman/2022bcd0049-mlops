stages:
  train:
    cmd: python train.py
    deps:
      - train.py
      - data/wine_v1.csv
      - data/wine_v2.csv
    params:
      - params.yaml:
          - train
    outs:
      - models/model.pkl
    metrics:
      - metrics.json:
          cache: false
